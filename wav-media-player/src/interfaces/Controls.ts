// src/interfaces/Controls.ts

export interface Controls {
    playButton: boolean;
    pauseButton: boolean;
    skipForwardButton: boolean;
    skipBackwardButton: boolean;
    repeatButton: boolean;
}